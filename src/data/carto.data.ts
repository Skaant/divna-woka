const EARTH_CIRCUM_METERS = 40000000;

const LONG_MIN = 5.672893;
const LONG_MAX = 5.677415;

export const CARTO_DATA = {
  LAT_MIN: 44.514354,
  LAT_MAX: 44.520106,
  LONG_MIN,
  LONG_MAX,
  MICRO_COORD_LAT_METERS:
    (EARTH_CIRCUM_METERS *
      Math.cos((((LONG_MIN + LONG_MAX) / 2) * Math.PI) / 180)) /
    360 /
    1000000,
  MICRO_COORD_LONG_METERS: EARTH_CIRCUM_METERS / 360 / 1000000,
  PIXELS_BY_METER: 1,
};
